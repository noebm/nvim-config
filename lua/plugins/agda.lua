return {
  "ashinkarov/nvim-agda",
  ft = {
    "lagda", "agda"
  },
  -- opts = {},
  config = true,
  --  function(plugin, opts)
  --  local agda = require("agda")
  --  agda.setup(opts)
  --  -- require("agda-input")
  --end
}
